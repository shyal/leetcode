Given a `triangle` array, return _the minimum path sum from top to bottom_.

For each step, you may move to an adjacent number of the row below. More formally, if you are on index `i` on the current row, you may move to either index `i` or index `i + 1` on the next row.

**Example 1:**

```
Input: triangle = [[2],[3,4],[6,5,7],[4,1,8,3]]
Output: 11
Explanation: The triangle looks like:
   2
  3 4
6 5 7
4 1 8 3
The minimum path sum from top to bottom is 2 + 3 + 5 + 1 = 11 (underlined above).
```

**Example 2:**

```
Input: triangle = [[-10]]
Output: -10
```

**Constraints:**

- `1 <= triangle.length <= 200`
- `triangle[0].length == 1`
- `triangle[i].length == triangle[i - 1].length + 1`
- `-10^4 <= triangle[i][j] <= 10^4`

**Follow up:** Could you do this using only `O(n)` extra space, where `n` is the total number of rows in the triangle?

## <!-- answer -->

I think this is a dp problem.

2
3 4
6 5 7
4 1 8 3

So we start at row 0, and ask what's the minimum path sum for row 1 and positions 0 and 1.
More generically we the minimum path sum for

```
T[r][c] = min(T[r+1][c], R[r+1][c+1])
```

Now we need to make it recursive, so the base case is when we reach the bottom of the array. So if c == len(T) -1

```

@cache
def func(r, c):
    if r >= len(T) or c >= len(T[r]):
        return 0
    return T[r][c] + min(func(r+1, c), func(r+1, c+1))
```

The code above may not be correct as it's pseudocode, but the recognition is seeing the recurrence relation and implementing it with recursion and memoization.

<!-- spot {"problem": "120", "target": "dp-1d-rolling", "reason": "failed to recognize last time, 158 problem(s) need only it", "seconds": 340} -->
