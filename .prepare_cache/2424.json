{"title": "Longest Uploaded Prefix", "code": "\"\"\"\nURL: https://leetcode.com/problems/longest-uploaded-prefix/description/?envType=problem-list-v2&envId=vn57k9wr\n\n2424. Longest Uploaded Prefix\n\nYou are given a stream of n videos, each represented by a distinct number from 1 to n that you need to \"upload\" to a server. You need to implement a data structure that calculates the length of the longest uploaded prefix at various points in the upload process.\n\nWe consider i to be an uploaded prefix if all videos in the range 1 to i (inclusive) have been uploaded to the server. The longest uploaded prefix is the maximum value of i that satisfies this definition.\n\nImplement the LUPrefix class:\n\n- LUPrefix(int n) Initializes the object for a stream of n videos.\n- void upload(int video) Uploads video to the server.\n- int longest() Returns the length of the longest uploaded prefix defined above.\n\nExample 1:\n\nInput\n[\"LUPrefix\",\"upload\",\"longest\",\"upload\",\"longest\",\"upload\",\"longest\"]\n[[4],[3],[],[1],[],[2],[]]\nOutput\n[null, null, 0, null, 1, null, 3]\n\nExplanation\nLUPrefix server = new LUPrefix(4);   // Initialize a stream of 4 videos.\nserver.upload(3);                    // Upload video 3.\nserver.longest();                    // Since video 1 has not been uploaded yet, there is no prefix.\n                                     // So, we return 0.\nserver.upload(1);                    // Upload video 1.\nserver.longest();                    // The prefix [1] is the longest uploaded prefix, so we return 1.\nserver.upload(2);                    // Upload video 2.\nserver.longest();                    // The prefix [1,2,3] is the longest uploaded prefix, so we return 3.\n\nConstraints:\n\n    1 <= n <= 10^5\n    1 <= video <= n\n    All values of video are distinct.\n    At most 2 * 10^5 calls in total will be made to upload and longest.\n    At least one call will be made to longest.\n\"\"\"\n\n\nclass LUPrefix:\n\n    def __init__(self, n: int):\n        pass\n\n    def upload(self, video: int) -> None:\n        pass\n\n    def longest(self) -> int:\n        pass\n\n\nsol = LUPrefix(4)\nsol.upload(3)\nprint(sol.longest())  # 0\n# sol.upload(1)\n# print(sol.longest())  # 1\n# sol.upload(2)\n# print(sol.longest())  # 3\n\n# assert (lambda: (sol := LUPrefix(4), sol.upload(3), sol.longest()))()[2] == 0\n# assert (lambda: (sol := LUPrefix(4), sol.upload(3), sol.upload(1), sol.longest()))()[\n#     3\n# ] == 1\n# assert (\n#     lambda: (\n#         sol := LUPrefix(4),\n#         sol.upload(3),\n#         sol.upload(1),\n#         sol.upload(2),\n#         sol.longest(),\n#     )\n# )()[4] == 3\n\n# assert (lambda: (sol := LUPrefix(1), sol.longest()))()[1] == 0\n# assert (lambda: (sol := LUPrefix(1), sol.upload(1), sol.longest()))()[2] == 1\n# assert (lambda: (sol := LUPrefix(5), sol.upload(5), sol.longest()))()[2] == 0\n# assert (lambda: (sol := LUPrefix(5), sol.upload(1), sol.longest()))()[2] == 1\n# assert (lambda: (sol := LUPrefix(5), sol.upload(2), sol.upload(1), sol.longest()))()[\n#     3\n# ] == 2\n# assert (\n#     lambda: (\n#         sol := LUPrefix(5),\n#         sol.upload(3),\n#         sol.upload(2),\n#         sol.upload(1),\n#         sol.longest(),\n#     )\n# )()[4] == 3\n# assert (\n#     lambda: (sol := LUPrefix(10**5), sol.upload(1), sol.upload(10**5), sol.longest())\n# )()[3] == 1\n# assert (\n#     lambda: (\n#         sol := LUPrefix(10**5),\n#         [sol.upload(i) for i in range(1, 10**5 + 1)],\n#         sol.longest(),\n#     )\n# )()[2] == 100000\n# assert (lambda: (sol := LUPrefix(10**5), sol.upload(10**5), sol.longest()))()[2] == 0\n# assert (lambda: (sol := LUPrefix(3), sol.upload(2), sol.upload(3), sol.longest()))()[\n#     3\n# ] == 0\n# assert (lambda: (sol := LUPrefix(3), sol.upload(3), sol.upload(1), sol.longest()))()[\n#     3\n# ] == 1\n", "solution": "\"\"\"\nURL: https://leetcode.com/problems/longest-uploaded-prefix/description/?envType=problem-list-v2&envId=vn57k9wr\n\n2424. Longest Uploaded Prefix\n\nYou are given a stream of n videos, each represented by a distinct number from 1 to n that you need to \"upload\" to a server. You need to implement a data structure that calculates the length of the longest uploaded prefix at various points in the upload process.\n\nWe consider i to be an uploaded prefix if all videos in the range 1 to i (inclusive) have been uploaded to the server. The longest uploaded prefix is the maximum value of i that satisfies this definition.\n\nImplement the LUPrefix class:\n\n- LUPrefix(int n) Initializes the object for a stream of n videos.\n- void upload(int video) Uploads video to the server.\n- int longest() Returns the length of the longest uploaded prefix defined above.\n\nExample 1:\n\nInput\n[\"LUPrefix\",\"upload\",\"longest\",\"upload\",\"longest\",\"upload\",\"longest\"]\n[[4],[3],[],[1],[],[2],[]]\nOutput\n[null, null, 0, null, 1, null, 3]\n\nExplanation\nLUPrefix server = new LUPrefix(4);   // Initialize a stream of 4 videos.\nserver.upload(3);                    // Upload video 3.\nserver.longest();                    // Since video 1 has not been uploaded yet, there is no prefix.\n                                     // So, we return 0.\nserver.upload(1);                    // Upload video 1.\nserver.longest();                    // The prefix [1] is the longest uploaded prefix, so we return 1.\nserver.upload(2);                    // Upload video 2.\nserver.longest();                    // The prefix [1,2,3] is the longest uploaded prefix, so we return 3.\n\nConstraints:\n\n    1 <= n <= 10^5\n    1 <= video <= n\n    All values of video are distinct.\n    At most 2 * 10^5 calls in total will be made to upload and longest.\n    At least one call will be made to longest.\n\"\"\"\n\n\nclass LUPrefix:\n\n    def __init__(self, n: int):\n        self.n = n\n        self.uploaded = [False] * (n + 1)\n        self.prefix = 0\n\n    def upload(self, video: int) -> None:\n        self.uploaded[video] = True\n        while self.prefix + 1 <= self.n and self.uploaded[self.prefix + 1]:\n            self.prefix += 1\n\n    def longest(self) -> int:\n        return self.prefix\n\n\nsol = LUPrefix(4)\nsol.upload(3)\nprint(sol.longest())  # 0\nsol.upload(1)\nprint(sol.longest())  # 1\nsol.upload(2)\nprint(sol.longest())  # 3\n\nassert (lambda: (sol := LUPrefix(4), sol.upload(3), sol.longest()))()[2] == 0\nassert (lambda: (sol := LUPrefix(4), sol.upload(3), sol.upload(1), sol.longest()))()[\n    3\n] == 1\nassert (\n    lambda: (\n        sol := LUPrefix(4),\n        sol.upload(3),\n        sol.upload(1),\n        sol.upload(2),\n        sol.longest(),\n    )\n)()[4] == 3\n\nassert (lambda: (sol := LUPrefix(1), sol.longest()))()[1] == 0\nassert (lambda: (sol := LUPrefix(1), sol.upload(1), sol.longest()))()[2] == 1\nassert (lambda: (sol := LUPrefix(5), sol.upload(5), sol.longest()))()[2] == 0\nassert (lambda: (sol := LUPrefix(5), sol.upload(1), sol.longest()))()[2] == 1\nassert (lambda: (sol := LUPrefix(5), sol.upload(2), sol.upload(1), sol.longest()))()[\n    3\n] == 2\nassert (\n    lambda: (\n        sol := LUPrefix(5),\n        sol.upload(3),\n        sol.upload(2),\n        sol.upload(1),\n        sol.longest(),\n    )\n)()[4] == 3\nassert (\n    lambda: (sol := LUPrefix(10**5), sol.upload(1), sol.upload(10**5), sol.longest())\n)()[3] == 1\nassert (\n    lambda: (\n        sol := LUPrefix(10**5),\n        [sol.upload(i) for i in range(1, 10**5 + 1)],\n        sol.longest(),\n    )\n)()[2] == 100000\nassert (lambda: (sol := LUPrefix(10**5), sol.upload(10**5), sol.longest()))()[2] == 0\nassert (lambda: (sol := LUPrefix(3), sol.upload(2), sol.upload(3), sol.longest()))()[\n    3\n] == 0\nassert (lambda: (sol := LUPrefix(3), sol.upload(3), sol.upload(1), sol.longest()))()[\n    3\n] == 1\n"}