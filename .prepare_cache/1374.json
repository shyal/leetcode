{"title": "Generate a String With Characters That Have Odd Counts", "code": "\"\"\"\nURL: https://leetcode.com/problems/generate-a-string-with-characters-that-have-odd-counts/description/?envType=problem-list-v2&envId=vn57k9wr\n\n1374. Generate a String With Characters That Have Odd Counts\n\nGiven an integer n, return a string with n characters such that each character in such string occurs an odd number of times.\n\nThe returned string must contain only lowercase English letters. If there are multiple valid strings, return any of them.\n\nExample 1:\n\nInput: n = 4\nOutput: \"pppz\"\nExplanation: \"pppz\" is a valid string since the character 'p' occurs three times and the character 'z' occurs once. Note that there are many other valid strings such as \"ohhh\" and \"love\".\n\nExample 2:\n\nInput: n = 2\nOutput: \"xy\"\nExplanation: \"xy\" is a valid string since the characters 'x' and 'y' occur once. Note that there are many other valid strings such as \"ag\" and \"ur\".\n\nExample 3:\n\nInput: n = 7\nOutput: \"holasss\"\n\nConstraints:\n\n    1 <= n <= 500\n\"\"\"\n\n\nclass Solution:\n    def generateTheString(self, n: int) -> str:\n        pass\n\n\nsol = Solution()\n\nprint(sol.generateTheString(4))  # \"pppz\" or any valid string with odd counts\n\n# assert len(sol.generateTheString(4)) == 4\n# assert all(\n#     sol.generateTheString(4).count(c) % 2 == 1 for c in set(sol.generateTheString(4))\n# )\n\n# assert len(sol.generateTheString(2)) == 2\n# assert all(\n#     sol.generateTheString(2).count(c) % 2 == 1 for c in set(sol.generateTheString(2))\n# )\n\n# assert len(sol.generateTheString(7)) == 7\n# assert all(\n#     sol.generateTheString(7).count(c) % 2 == 1 for c in set(sol.generateTheString(7))\n# )\n\n# assert (\n#     all(\n#         sol.generateTheString(1).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(1))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(500).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(500))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(3).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(3))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(10).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(10))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(11).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(11))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(499).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(499))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(250).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(250))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(251).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(251))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(4).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(4))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(5).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(5))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(6).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(6))\n#     )\n#     == True\n# )\n# assert (\n#     all(\n#         sol.generateTheString(7).count(c) % 2 == 1\n#         for c in set(sol.generateTheString(7))\n#     )\n#     == True\n# )\n", "solution": "\"\"\"\nURL: https://leetcode.com/problems/generate-a-string-with-characters-that-have-odd-counts/description/?envType=problem-list-v2&envId=vn57k9wr\n\n1374. Generate a String With Characters That Have Odd Counts\n\nGiven an integer n, return a string with n characters such that each character in such string occurs an odd number of times.\n\nThe returned string must contain only lowercase English letters. If there are multiple valid strings, return any of them.\n\nExample 1:\n\nInput: n = 4\nOutput: \"pppz\"\nExplanation: \"pppz\" is a valid string since the character 'p' occurs three times and the character 'z' occurs once. Note that there are many other valid strings such as \"ohhh\" and \"love\".\n\nExample 2:\n\nInput: n = 2\nOutput: \"xy\"\nExplanation: \"xy\" is a valid string since the characters 'x' and 'y' occur once. Note that there are many other valid strings such as \"ag\" and \"ur\".\n\nExample 3:\n\nInput: n = 7\nOutput: \"holasss\"\n\nConstraints:\n\n    1 <= n <= 500\n\"\"\"\n\n\nclass Solution:\n    def generateTheString(self, n: int) -> str:\n        if n % 2 == 1:\n            return \"a\" * n\n        else:\n            return \"a\" * (n - 1) + \"b\"\n\n\nsol = Solution()\n\nprint(sol.generateTheString(4))  # \"pppz\" or any valid string with odd counts\n\nassert len(sol.generateTheString(4)) == 4\nassert all(\n    sol.generateTheString(4).count(c) % 2 == 1 for c in set(sol.generateTheString(4))\n)\n\nassert len(sol.generateTheString(2)) == 2\nassert all(\n    sol.generateTheString(2).count(c) % 2 == 1 for c in set(sol.generateTheString(2))\n)\n\nassert len(sol.generateTheString(7)) == 7\nassert all(\n    sol.generateTheString(7).count(c) % 2 == 1 for c in set(sol.generateTheString(7))\n)\n\nassert (\n    all(\n        sol.generateTheString(1).count(c) % 2 == 1\n        for c in set(sol.generateTheString(1))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(500).count(c) % 2 == 1\n        for c in set(sol.generateTheString(500))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(3).count(c) % 2 == 1\n        for c in set(sol.generateTheString(3))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(10).count(c) % 2 == 1\n        for c in set(sol.generateTheString(10))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(11).count(c) % 2 == 1\n        for c in set(sol.generateTheString(11))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(499).count(c) % 2 == 1\n        for c in set(sol.generateTheString(499))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(250).count(c) % 2 == 1\n        for c in set(sol.generateTheString(250))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(251).count(c) % 2 == 1\n        for c in set(sol.generateTheString(251))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(4).count(c) % 2 == 1\n        for c in set(sol.generateTheString(4))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(5).count(c) % 2 == 1\n        for c in set(sol.generateTheString(5))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(6).count(c) % 2 == 1\n        for c in set(sol.generateTheString(6))\n    )\n    == True\n)\nassert (\n    all(\n        sol.generateTheString(7).count(c) % 2 == 1\n        for c in set(sol.generateTheString(7))\n    )\n    == True\n)\n"}