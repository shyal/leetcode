{
  "lcunionfind": {
    "prefix": "lcunionfind",
    "body": [
      "def find(x):",
      "    if x != parent[x]:",
      "        parent[x] = find(parent[x])",
      "    return parent[x]",
      ""
    ]
  },
  "lcunionunion": {
    "prefix": "lcunionunion",
    "body": [
      "def union(x, y):",
      "    rootx = find(x)",
      "    rooty = find(y)",
      "    if rootx != rooty:",
      "        parent[rootx] = rooty",
      ""
    ]
  },
  "lcgetdigits": {
    "prefix": "lcGetDigits",
    "body": [
      "def getDigits(self, num):",
      "    digits = []",
      "    while num:",
      "        digits.append(num % 10)",
      "        num //= 10",
      "    return digits",
      ""
    ]
  },
  "argparse": {
    "prefix": "argparse",
    "body": [
      "import argparse",
      "",
      "parser = argparse.ArgumentParser(",
      "    description=\"Predict time to solve remaining LeetCode questions based on past solve rate.\"",
      ")",
      "parser.add_argument(",
      "    \"--total\", type=int, required=True, help=\"Total number of questions.\"",
      ")",
      "",
      "args = parser.parse_args()",
      "",
      "total = args.total",
      ""
    ]
  },
  "lctobase": {
    "prefix": "lctoBase",
    "body": [
      "def toBase(self, n, k):",
      "    res = []",
      "    while n:",
      "        n, m = divmod(n, k)",
      "        res = [m] + res",
      "    return reduce(lambda acc, val: acc * 10 + val, res)",
      ""
    ]
  },
  "lccomb": {
    "prefix": "lccomb",
    "body": [
      "def helper(i${3}):",
      "    if ${1:len(curr) == k}:",
      "        ret.append(curr[:])",
      "        return",
      "    for j in range(i, len(nums)):",
      "        curr.append(nums[j])",
      "        helper(${2:j + 1}${3})",
      "        curr.pop()",
      "",
      "ret = []",
      "curr = []",
      "helper(0${3})",
      "return ret",
      ""
    ],
    "description": "combinations by start index"
  }
}
