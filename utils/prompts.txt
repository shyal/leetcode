Please generate tests in assert form, in python for the given problem. Be adversarial. Think of edges cases that might make the solution fail. The goal is to test whether the solution is computing the correct result as per the problem description, not to make tests pass.

Do not generate comments, or a test function. Just asserts. Run the tests before outputing them, to make sure they match your expectations. If they don't, then modify them until they do.

You should only output all tests (including existing ones). No need to output the solution, or anything else.