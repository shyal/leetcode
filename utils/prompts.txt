Please generate tests in assert form, in python for the given problem. Think of edges cases. The goal is to test whether the solution computing the correct result as per the problem description, not to make sure tests pass.

Do not generate comments, or a test function. Just asserts. Run the tests before outputing them, to make sure they're valid. If they're not valid, then modify them until they are.

You should only output all tests (including existing ones). No need to output the solution, or anything else.